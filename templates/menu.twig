{# MENU COMES FROM menu.twig #}
{# Menu Items#}

{% if menu %}
	<ul class="first:flex first:place-content-around *:even:invisible li:a:visible">
		{% for item in items %}
			<li class="{{ item.classes | join(' ') }} group">
				<a class="group-hover:visible place-content-around" target="{{ item.target }}" href="{{ item.link }}">{{ item.title }}</a>
				{% include "menu.twig" with {'items': item.children} %}
			</li>
		{% endfor %}
	</ul>
{% endif %}
